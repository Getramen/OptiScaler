interface:
  display_name: "OptiScaler"
  short_description: "Repo-specific patterns and workflows for OptiScaler"
  default_prompt: "Use the OptiScaler repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true